set n 5
set i 1

while {$i <= $n} {

    set s 1
    while {$s <= [expr {$n - $i}]} {
        puts -nonewline " "
        incr s
    }

    set j 1
    set num 1
    while {$j <= [expr {2*$i - 1}]} {
        puts -nonewline $num
        incr num
        incr j
    }

    puts ""
    incr i
}